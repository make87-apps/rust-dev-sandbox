version: 1
config:
  type: object
  properties:
    my_list:
      type: array
      items:
        type: string
build:
  build_kit:
    name: rust
dev_build:
  build_kit:
    name: rust
    build_additions:
      ide: vscode_server_rust
      ssh: ssh_server